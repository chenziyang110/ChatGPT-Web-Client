# ChatGPT Web Client v1.4.9

修复会话队列在消息发送后遇到错误或超时，仍要求人工核对、导致下一条无法发送的问题。

- 网页报错、回复中断或本地等待超时后，已尝试发送的消息记为失败，不会重复发送；同一会话队列自动等待网页恢复可发送状态，再发送下一条。
- 更新后自动解除旧版本因这类错误留下的“需核对”暂停；用户主动暂停或人工接管的队列保持原状态。
- 发送前遇到登录、验证、草稿冲突等无法安全发送的情况，仍等待用户处理。
