# ChatGPT Web Client v1.4.11

修复网页区域突然空白、切换会话后无法看到页面的问题。

- 当前 ChatGPT 网页视图意外销毁时，客户端会清理失效对象并自动恢复原标签页；待发送队列及其暂停状态保留。
- 若网页在已发送的排队消息回复期间中断，原会话标签仍保留；当前消息按中断结束，下一条待发消息会继续发送，原消息不会重发。
- 如果网页画面仍未恢复，工作区显示“恢复页面”按钮，不再留下一整块空白。
- “网页对象已销毁”和“客户端正在关闭”分别提示，避免误导。
- 修复销毁后的清理逻辑再次读取失效网页对象而引发的主进程 JavaScript 异常。
