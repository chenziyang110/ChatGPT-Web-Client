# ChatGPT Web Client v1.4.10

修复从旧版本更新时，会话队列仍停在“消息已发送，回复需核对”的问题。

- 旧客户端在安装更新前可能把自动报错暂停改写成“应用关闭，队列已暂停”。新版本可恢复这类已发送、已中断的排队任务，跳过该条并在网页可发送后继续下一条；不会重复发送原消息。
- 今后的退出与更新会保留队列原有的暂停原因，避免覆盖错误状态或用户手动暂停。
- 人工接管、主动取消，以及尚未发送的任务仍按原有保护规则处理。
