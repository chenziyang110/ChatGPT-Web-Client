# ChatGPT Web Client v1.4.7

修复完成的图片回复仍让会话队列等待，并改进窗口关闭和多账号标签恢复。

- 图片回复生成完成后，即使回复没有文字，也会在确认停止生成后结束本轮，依次发送后续排队消息。侧栏或已完成图片组件上的忙碌标记不会误判为当前回复仍在生成。
- 点击窗口右上角 × 后隐藏到系统托盘，账号页面和会话队列继续运行；从托盘菜单选择「退出」才完全关闭客户端。
- 完全退出再打开后，恢复每个账号打开的会话标签及各自选中的标签。未选中的标签按需加载，已手动关闭的标签不会重新出现。
