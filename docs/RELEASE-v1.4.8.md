# ChatGPT Web Client v1.4.8

修复 ChatGPT 停止生成后，会话队列仍停在“等待回复”的问题。

- 识别“连接已中断。正在等待完整回复”，本轮记为失败并继续发送同一会话队列中的下一条。
- 输入框已恢复可用、生成活动持续停止时，即使网页没有完整回复或明确错误提示，也会结束本轮；已发送的消息不会重复发送。
- 手动发送的会话遇到上述停止状态时，也会释放等待中的队列。未完成的答复不会误标为成功或未读回复。
- 对需要人工核对的其他网页错误，仍暂停对应会话队列。
