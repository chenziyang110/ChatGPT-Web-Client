# ChatGPT Web Client v1.4.13

修复 ChatGPT 将“无法思考”显示为普通文字时，客户端没有将其识别为本轮结束的问题。

- 本轮出现“无法思考”且回复已经停止后，当前排队消息记为失败，下一条自动发送，不再等待人工处理。
- 兼容“无法思考”的按钮和纯文字样式；历史提示与已完成回复不会误触发。
- 包含 v1.4.12 的目标页面操作失败自动跳过和旧队列阻塞恢复。使用 v1.4.10 等旧版时，需要安装新版本并重启客户端才能生效。
