import React from 'react';
import { createRoot } from 'react-dom/client';

function App(){
 return <div>ChatGPT-Web-Client Runtime</div>;
}

createRoot(document.getElementById('root')!).render(<App/>);
