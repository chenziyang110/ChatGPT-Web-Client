# ChatGPT Web Client v1.4.6

修复 ChatGPT 显示“无法思考”后队列等待不动，以及多账号切换后只读预览画面停止更新的问题。

- 当前网页的“无法思考”状态在停止生成并稳定确认后，视为本轮结束；队列自动发送下一条消息。
- 若失败的是队列已发送的消息，该条记录为失败，不会重发；同一会话后续消息继续执行。
- 只识别当前用户消息之后的失败状态。历史记录、引用文字和仍在生成的页面不会触发自动发送；其他网页错误仍按原规则暂停队列。
- 只读预览抓图超时后自动重试；当会话内容在更新而画面不变时，唤醒隐藏页面重新抓图。切换账号再返回可继续看到最新画面。
