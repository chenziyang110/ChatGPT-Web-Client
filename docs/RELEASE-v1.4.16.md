# v1.4.16

修复长回复已结束但会话队列一直显示「已发送，等待回复」的问题。

- GPT 网页可能在长回复后移除已发送提问的页面元素。队列现在保留发送确认，在同一会话的回复结束、输入框空闲且状态稳定后继续下一条。
- 重启后恢复已确认发送的任务时同样支持此情况，不重复发送原消息。
- 不把无法关联的可见回复冒充为本任务的回答；严格的 Agent 回复读取仍保留消息身份校验。
- 用户主动暂停、接管，以及正在生成、页面加载、未发送草稿等状态保持原有处理。
